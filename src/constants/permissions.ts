import { ROLES } from "./roles";

export const PERMISSIONS = {
    CUSTOMER: [
        ROLES.CUSTOMER,
    ],

    STAFF: [
        ROLES.STAFF,
    ],

    MANAGER: [
        ROLES.MANAGER,
    ],

    ADMIN: [
        ROLES.ADMIN,
    ],
};